{
  "permissions": {
    "allow": [
      "Bash(git add:*)",
      "Bash(git commit -m \"$\\(cat <<''EOF''\nfix: preserve invitation aspect ratio between web editor and PDF output\n\nSeparate preview into .sie-preview-area \\(outer container\\) and .sie-canvas\n\\(inner element\\) so the canvas maintains the exact aspect ratio from JSON\nconfig. Use ResizeObserver + fitCanvas\\(\\) to dynamically scale the canvas\nto fit the available space when the modal opens or the window resizes.\n\nCo-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>\nEOF\n\\)\")",
      "Bash(git push:*)"
    ]
  }
}
